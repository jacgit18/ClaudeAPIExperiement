# Agents

Generated by `build_agent.py` from the specs in `../specs/`. Files here are build
output, not hand-written — if the behavior needs to change, edit the spec and
re-run the builder rather than hand-editing the generated code, so the two don't
drift apart.
