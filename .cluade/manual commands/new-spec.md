---
description: Scaffold a new agent spec file from a one-line description
---

Create a new file at `specs/$1.md` following the format documented in
`specs/README.md` (Purpose, Interface, Behavior, Model). Base the content on this
description: $ARGUMENTS

Don't run build_agent.py yet — just write the spec and show it for review.
